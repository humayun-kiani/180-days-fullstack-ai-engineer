# k8s/base/secret.yaml
# Sensitive configuration
# In production: use Sealed Secrets, HashiCorp Vault, or AWS Secrets Manager
# NEVER commit real secrets to git — this is a template only!

apiVersion: v1
kind: Secret
metadata:
  name: task-api-secrets
  namespace: task-api
  labels:
    app: task-api
type: Opaque
data:
  # Values must be base64 encoded: echo -n "value" | base64
  # These are placeholder values — replace before production use
  ANTHROPIC_API_KEY: cGxhY2Vob2xkZXI= # "placeholder"
  DATABASE_URL: cGxhY2Vob2xkZXI= # "placeholder"
